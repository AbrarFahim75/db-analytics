# Lab 1 — SQL Fundamentals (DDL & DML)

**Course:** Databases IE4, HAW Hamburg, SoSe 2025  
**Database:** PostgreSQL  
**Schema:** lab1_2 (Shipping Company)

## Assignments

1. **Student Information System** — CREATE TABLE with constraints, INSERT sample data
2. **Shipping Company** — SQL queries (SELECT, WHERE, JOIN, GROUP BY) on Harbour/Sailor/Ship/Hire
3. **COMPANY example** — Queries on the Elmasri textbook schema
4. **Relational Algebra vs. SQL** — Cinema database queries

## Dataset

The shipping database script is located at [`datasets/shipping.sql`](../../datasets/shipping.sql)

## Key Concepts Practiced

- CREATE TABLE with PRIMARY KEY, FOREIGN KEY, NOT NULL
- INSERT INTO with VALUES
- SELECT with WHERE, BETWEEN, LIKE, IN
- INNER JOIN and LEFT JOIN
- GROUP BY with COUNT, SUM
- Query interpretation (reading SQL and explaining in natural language)
```

Commit message: `Add Lab 1 README with assignment overview`

### Step 2: Create `labs/lab2/README.md`

Same process. Click **"Add file"** → **"Create new file"**. Filename:
```
labs/lab2/README.md
